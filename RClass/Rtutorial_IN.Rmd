---
title: "R Tutorial"
author: "Sophie Wulfing"
date: "2024-01-20"
output: html_document
---
# R STUDIO

## 4 PANEL
### KIRI ATAS - PANEL SUMBER
- Menulis dan menyimpan skrip skrip kode sehingga analysis bisa dilakukan kembali

### KIRI BAHWA - KONSOLE
- Hasil atau keluaran kode. Bisa menjalankan kode di sini kalau kamu mau memeriksa kodenya atau berinteraksi dengan data.

### KANAN ATAS
- Environment/linkugan punya variabel variabel dibuat oleh Anda

### KANAN BAHWA
- Tab tab: Files - Files bisa dieksplorasi
  Plots/ grafik: Visualisasi
  Packages: Di sini kamu bisa metemukan paket kode dibuate oleh orang lain
    -Banyak fungsi: genetika, menganalisis matriks, membuat peta, menyelesaikan persamaan, dll
  Help: mengakses library
    CONTOH - bisa mengetik ?NAMAPAKET di panel sumber atau panel konsole dan bisa membaca tentang paket, input input, contoh, dan kegunaan paketnya

# BAGAIMANA MEMBUAT DOKUMEN DAN MULAI ANALISIS
- File => New file => R script
- Cara cara kode diaktifkan
  Blok, "Code", "Run"
  "Run" Tombol di kanan atas
  CTRL + ENTER
  
``` {r OPERATOR DASAR}
#Perhitungan dasar dapat diselesaikan
2 + 3

#Pembagian
94729 / 7

# Pembagian utuh- desimal tidak dipakai
94729 %/% 7

#Aritmatika modular - keluaran adaleh sisanya
94729 %% 7

# BISA membuat komentar - kata kata tidak dijalankan - dengan tanda #
# SELALU DIBERI LABEL PADA KODE. 

#FUNGSI CETAK

print("Hello, world")

#TO DO: MAKE CHEAT SHEET W OPERATORS
```

# VARIABEL
METODE SIMPAN DATA - memberi nama pada data atau objek

 - HARUS mulai dengan huruf
 - huruf/angka/titik/garis bahwa bisa dipakai
 - Harus cek huruf besar atau huruf kecil
 - Tidak bisa pakai nama: TRUE, FALSE, NULL, if. Sudah nama fungsi di R 
 
 <!-- karena itu adalah fungsi -->
 
```{r Variabel}

## Nama variabel yang tidak berjalan
# 1nama <- "Sophie"
# .1nama <- "Sophie"
# _1nama <- "Sophie"
# nama 1 <- "Sophie"

#Huruf besar atau huruf kecil
nama1 <- "Sophie"
NAMA1

# Bisa membuat variabel dara nomor dan membuat kalkulasi
x <- 4
y <- 17

x*y

# Membuat variabel dari hasil kalkulasi
z <- x * y
z


#Bersatu rangkian
Negara <- "Indonesia"
Umur <- 27

paste("Halo, nama saya", nama1, "Saya dari", Negara, "Umur saya", Umur, "tahun") # Harus pakai koma di antara kata dan variabel


#TO DO: MAKE A PRACTICE SHEET OF THIS
```
# JENIS DATA
- Nomor
- Angka utuh
- Boolean/logical
- Karakter/huruf
- Rangkian
- Factor - Lewati untuk saat ini. Seperti Rangkian tapi pakai hirarki

```{r JenisData}

# NUMERIC - tidak pakai tanda kutip
k <- "17"
x * k

# Cek jenis data dengan "class"
class(k)

# Menrubah jenis data
k <- as.numeric(k)


class(k)
x * k #Berjalan

# BOOLEAN: TRUE OR FALSE (Betul atau Salah). Di worksheet
x < k
x == k #harus pakai dua tanda sama dengan


# Karakter dan rangikan

#Menghitung
nchar(name1)

# Cek kalau ada kata di dalam rangikan
grepl("Soph", name1)
grepl("Anne", name1)


```

# DAFTAR
Koleksi objek diatur (DOES THAT MEAN ORDERED OR JUST ORGANIZED)
Berberapa objek bisa disimipan di satu variabel


```{r Daftar}

# MEMBUAT DAFTAR

buah <- c("apel", "pisang", "sangka", "salak", "mangga")
Daftar_1 <- c(4, 546, 234, 85, 1, 8, 3543, 654, 14, 46, 8934)


OneToTen <- 1:10 # Membuat dafar nomor hitung dengan tanda titik dua
evenNumbers <- seq(0, 20, 2) #kalau tidak mau setiap nomor, bisa membuat urutan dengan seq
tenOnes <- rep(3, 5) #Membuat dafter di nomor sama berkali-kali

# Dafter Isi
min(Daftar_1) #Minimum
max(Daftar_1) #Maksimum
sort(Daftar_1) #Mengatur
length(Daftar_1) #Ukurang
range(Daftar_1) #Minimum dan Maksimum

#Menganggil bagian daftar
fruit[1]
fruit[1:3]
fruit[c(1,3, 5)]
fruit[-2]

#Merubah bagian daftar
fruit[1] <- "jeruk"
fruit

#operator boolean - cek daftar
"apel" %in% fruit

#Menambah input baru pada daftar
fruit_2 <- append(fruit, "apel", after = 2)
fruit_2

```
 # MATRICES, DATA FRAMES, AND NESTED LISTS
 
 Two or higher dimensional datasets with rows and columns
 
```{r matrices}

# Create a matrix
numbersMatrix <- matrix(c(1,2,3,4,5,6), nrow = 2, ncol = 3)

# Print the matrix
numbersMatrix

# Can be done with strings as well
fruitMatrix <- matrix(c("apple", "banana", "cherry", "mango","grape", "pineapple", "pear", "melon", "fig"), nrow = 3, ncol = 3)

fruitMatrix

# Indexing
fruitMatrix[1, 2]
fruitMatrix[2,]
fruitMatrix[,2]

#More than one row or column
fruitMatrix[c(1,2),]
fruitMatrix[,c(1,2)]


# ADDING COLUMNS
newFruitMatrix_1 <- cbind(fruitMatrix, c("strawberry", "blueberry", "raspberry"))

# Print the new matrix
newFruitMatrix_1

#ADDING ROWS
newFruitMatrix_2 <- rbind(fruitMatrix, c("strawberry", "blueberry", "raspberry"))

# Print the new matrix
newFruitMatrix_2

# REMOVING ROWS AND COLUMNS
fruitMatrix <- fruitMatrix[-c(1), -c(1)]

fruitMatrix

# check if in matrix
"apple" %in% fruitMatrix
"grape" %in% fruitMatrix

# Check Sizes
dim(fruitMatrix)
length(fruitMatrix)

```

```{r dataframes}

# Create a data frame
Data_Frame <- data.frame (
  Student = c("Rina", "Virginia", "Ben"),
  Score = c(100, 150, 120),
  Time = c(60, 30, 45)
)

# Print the data frame
View(Data_Frame)

summary(Data_Frame)

#Accessing
View(Data_Frame[1])

Data_Frame[["Student"]]

Data_Frame$Student


# Add a new row
New_row_DF <- rbind(Data_Frame, c("John", 110, 110))

# Print the new row
View(New_row_DF)

# Add a new column
New_col_DF <- cbind(Data_Frame, Place = c(2, 1, 3))

# Print the new column
View(New_col_DF)

# Remove the first row and column
Data_Frame_Remove <- Data_Frame[-c(1), -c(1)]

# Print the new data frame
View(Data_Frame_Remove)

# Dimensions
dim(Data_Frame)
ncol(Data_Frame)
nrow(Data_Frame)
length(Data_Frame) #same as ncol
```
 
 # IF STATEMENTS
 
 Will execute code only if a statement is true
 
```{r ifstatement}
#if statement
a <- 33
b <- 200

if (b > a) { #Boolean statement. If true, will execute what's in the curly brackets
  print("b is greater than a")
}

# else if statement - run if the previous statement is untrue

a <- 33
b <- 33

if (b > a) {
  print("b is greater than a")
} else if (a == b) {
  print ("a and b are equal")
}

# Else statement - if no previous statements are true

a <- 200
b <- 33

if (b > a) {
  print("b is greater than a")
} else if (a == b) {
  print("a and b are equal")
} else {
  print("a is greater than b")
}

```

``` {r nestedifstatement}

# Else statement SHOW RUSSIAN DOLL EXAMPLE

a <- 200
b <- 33

if (a < 1000){
  if(b < 1000) {
  print("both a and b are below 1000")
  } 
}



```


``` {r qualifstatement}

# 2 statement

a <- 200
b <- 33

if (a < 100 & b < 100) {
  print("both a and b are below 100")
} 

if (a < 100 | b < 100) {
  print("either a or b are below 100")
} 


```

# WHILE LOOPS

Will execute a code as long as a condition is true

```{r whileloops}

i <- 1
while (i <= 6) {
  print(i)
  i <- i + 1
}

```
 
# FOR LOOPS

Iterates over a sequence

```{r forLoops}
for (x in 1:10) {
  print(x)
}


fruits <- list("apple", "banana", "cherry")

for (x in fruits) {
  print(x)
}

```


# FUNCTIONS

A block of code that is run only when you call it

```{r functions}

my_function <- function() { # create a function with the name my_function
  print("Hello World!")
}

# Have to call the function to use it
my_function()

# Function with arguments

name_function <- function(name){
  print(paste("Hello, my name is", name))
}

name_function()
name_function("Sophie") #Now can change argument to change the output

# Multiple arguments
intro_function <- function(name, home){
  print(paste("Hello, my name is", name, "I am from", home))
}

intro_function("Sophie", "Washington")


# Function with return values - return is used more often because it stores a variable
mult_function <- function(x) {
  return (5 * x)
}

print(mult_function(3))
print(mult_function(5))
print(mult_function(9))

FinalScore <- mult_function(9) * 100
FinalScore
```
 
 
 
 
 
 
 
 
 
 
 
 
 
 